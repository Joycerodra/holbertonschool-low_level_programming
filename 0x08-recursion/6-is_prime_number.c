#include "main.h"

/**
 * is_prime_number - return 1 if the int number is prime
 * @n: is the number
 * Return: return 1 if the number is prime or 0 otherwise
 **/

int is_prime_number(int n)
{
	if (n < 2)
		return (0);

	return (n);
}
